Restore the pom.xml.<bk_ext> to pom.xml

```
restorePomFile()
```

or

```
restorePomFile(ext: .bk)
```

or

```
restorePomFile(ext: .bk, remove: true)
```

* *ext*: The extension to be used to restore the backuped pom.xml file
* *remove*: Delete the backup file
