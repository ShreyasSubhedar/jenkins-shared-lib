TODO. To be defined and explainex about the purpose of this lib and how to use it

```
buildJavaApp()
```

```
buildJavaApp(
      repo: https://github.com/snowdrop/rest-http-example.git,
      branch: "2.4.3-1")
```

```
buildJavaApp(
      repo: https://github.com/snowdrop/rest-http-example.git,
      branch: "2.4.3-1",
      deploy: false)
```

* *repo*: the repository to use.
* *branch*: the branch to checkout from the repo.
* *deploy*: boolean to test if `mvn package` step should be done. Default: false

_NOTE_: 'shallow' is forced to be disabled when running on Pull Requests
